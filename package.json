{
  "name": "dsa-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Local dashboard for Markdown-based DSA practice",
  "scripts": {
    "start": "npm run build:client && nodemon server.js",
    "build:client": "npm --prefix client run build",
    "dev:client": "npm --prefix client run dev",
    "dev:server": "node --watch server.js",
    "lint": "npm --prefix client run lint"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
